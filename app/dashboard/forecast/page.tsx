import ForecastAdvisoryPage from "@/components/dashboard/pages/ForecastAdvisoryPage";

/**
 * Live weather planning workspace. Its interactive implementation lives in the
 * focused page component so this App Router route remains small and stable.
 */
export default function ForecastPage() {
  return <ForecastAdvisoryPage />;
}
